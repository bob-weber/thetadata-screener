# Credentials — never commit
creds.txt

# ThetaData terminal binary and downloaded server jars
ThetaTerminalv3.jar
lib/

# Scan cache files (regenerated on each run)
*_cache.json
options_results_cache.json

# User data files (personal positions / ticker lists)
my_option_positions.json
my_stock_positions.json
my_option_superseded.json
my_option_events.json
my_option_events_*.json
my_positions.txt
reject_list.txt
gains_history.json
gains_history_*.json

# Logs
*.log

# Python virtual environments
gui-env/
thetadata-env/

# Python cache
__pycache__/
*.pyc
*.pyo

# Claude Code local settings
.claude/

# Editor / OS
.DS_Store
*.swp
